# Lander environment — intentionally isolated from the main site.
# NO shared main-site secrets (no Supabase project keys, no service-role key).
# Only lander-specific, domain-restricted public values belong here.
